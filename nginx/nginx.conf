# Placeholder — only needed if deploying with a separate nginx reverse proxy.
# In the default Fly.io setup, Flask serves static files directly (single image).

# server {
#     listen 80;
#     server_name _;
#
#     location /api/ {
#         proxy_pass http://backend:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         # Required to prevent nginx buffering SSE streams
#         proxy_buffering off;
#         proxy_cache off;
#     }
#
#     location / {
#         root /usr/share/nginx/html;
#         try_files $uri $uri/ /index.html;
#     }
# }
